public class atalhosJava {
    public static void main(String[] args) {

        // OPERADORES DE ATRIBUIÇÃO CUMULATIVA
        // a += b; ------- a = a + b;
        // a -= b; ------- a = a - b;
        // a *= b; ------- a = a * b;
        // a /= b; ------- a = a / b;
        // a %= b; ------- a = a % b;

        // QUANDO USAR O FOR
        // QUANDO SE SABE PREVIAMENTE A QUANTIDADE DE REPETIÇÕES OU O INTERVALO DOS
        // VALORES

        // O TERMO "CAMEL CASE" É USADO QUANDO FORMATAMOS EM JAVA O MODELO DE
        // INICIAR UMA PALAVRA MINUSCULA E DPS MAISUCULA, COMO: darkSouls
        // PASCAL CASE: DarkSouls

        // Camel Case = pacotes, atributos, métodos, váriaveis e parametros
        // Pascal Case = classes
    }
}
