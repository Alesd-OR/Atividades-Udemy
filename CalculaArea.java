public class CalculaArea {
    public static void main(String[] args) {

        double input = 2.00;
        double formula;
        double area;

        formula = Math.pow(input, 2) * 3.14159;

        area = formula;

        System.out.printf("O valor da área pré estabelecida é de %.4f: ", area);

    }
}
